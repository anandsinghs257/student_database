<div class="container">
  <div class="card mt-2">
    <div class="card-header">
      <h1>New Student</h1>
    </div>
    <div class="card-body">
      <%= render 'form', student: @student %>

      <%= link_to 'Back', students_path, class: "btn btn-primary mt-2 btn-sm" %>
    </div>
  </div>
</div>
