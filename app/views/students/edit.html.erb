<div class="container">
  <div class="card mt-2 mb-4">
    <div class="card-header">
      <h1>Editing Student</h1>
    </div>
    <div class="card-body">
      <%= render 'form', student: @student %>

      <%= link_to 'Show', @student, class: "btn btn-success btn-sm mt-2" %>
      <%= link_to 'Back', students_path, class: "btn btn-secondary btn-sm mt-2" %>
    </div>
  </div>
</div>

